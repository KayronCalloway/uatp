# Legacy Fix Scripts

This folder contains one-off debug and hot-fix scripts created during the Phase-1 bug-hunt (e.g. `fix_*` files).

They are archived here to preserve history while keeping the repository root clean.  **These scripts are not part of the supported production code path.**

If a fix is still valuable, please extract its logic into proper unit tests or documented utilities and delete the script.
