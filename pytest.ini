[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
norecursedirs =
    .*
    archive
    build
    dist
    node_modules
    sdk
    tests/legacy
    tests/performance
markers =
    network: marks tests as requiring network access (deselect with '-m "not network"')
